pipeline {
    agent any
    stages {
        stage('Greeting') {
            steps {
                sh '''
                echo "Hello, welcome to my Jenkins"
                '''
            }
        }
        stage('Pull repo') {
            steps {
                sh '''
                echo "The next step"
                '''
            }
        }
        stage('Flask-app triggers') {
            steps {
                sh '''
                echo "try to run the hello file - or not. whatever"
                '''
            }
        }
        stage('Database triggers') {
            steps {
            sh '''
            hostname
            '''
            }
        }
        stage('NGINX tings') {
            steps {
            sh '''
            echo "This is actually, actually working 14:12 26/9/23 NNAI"
            echo "The previous step establishes remote connection nd closes it"
            '''
            }
        }
                stage('Networks') {
            steps {
            sh '''
            echo "This is actually, actually working 14:12 26/9/23 NNAI"
            echo "The previous step establishes remote connection nd closes it"
            '''
            }
        }
    }
}